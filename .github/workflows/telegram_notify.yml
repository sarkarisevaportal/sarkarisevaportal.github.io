name: Send Sarkari Updates to Telegram
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check-rss:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch RSS and Post to Telegram
        run: |
          FEED_URL="https://www.indiagovtjobs.in/feeds/posts/default"
          BOT_TOKEN="${{ 7953477032:AAH1vJkMDNHYkzDS4WSya2MWBT25kGShS8k }}"
          CHANNEL_USERNAME="@sarkarisevaportaljkssb"

          # Python se sahi parsing
          RESULT=$(python3 - <<'EOF'
import urllib.request
import xml.etree.ElementTree as ET

url = "https://www.indiagovtjobs.in/feeds/posts/default"
req = urllib.request.urlopen(url, timeout=10)
tree = ET.parse(req)
root = tree.getroot()
ns = {'atom': 'http://www.w3.org/2005/Atom'}

entry = root.find('atom:entry', ns)
title = entry.find('atom:title', ns).text
link = entry.find("atom:link[@rel='alternate']", ns).get('href')
print(f"{title}||{link}")
EOF
)

          TITLE=$(echo "$RESULT" | cut -d'||' -f1)
          LINK=$(echo "$RESULT" | cut -d'||' -f2)

          MESSAGE="📌 *Nayi Sarkari Update*%0A%0A${TITLE}%0A%0A🔗 ${LINK}"

          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d "chat_id=${CHANNEL_USERNAME}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=Markdown"
